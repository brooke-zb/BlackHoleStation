<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="top.brookezb.bhs.mapper.ArticleMapper">
    <resultMap id="articleMap" type="top.brookezb.bhs.model.Article">
        <id property="aid" column="aid"/>
        <result property="uid" column="uid"/>
        <result property="title" column="title"/>
        <result property="description" column="description"/>
        <result property="picture" column="picture"/>
        <result property="content" column="content"/>
        <result property="commentabled" column="commentabled"/>
        <result property="appreciatabled" column="appreciatabled"/>
        <result property="created" column="created"/>
        <result property="modified" column="modified"/>
        <result property="status" column="status"/>
        <result property="views" column="views"/>
        <association property="category">
            <id property="cid" column="cid"/>
            <result property="name" column="name"/>
        </association>
    </resultMap>

    <insert id="insert">
        insert into bhs_article(aid, uid, cid, title, description, picture, content, commentabled, appreciatabled,
                                created, status)
        values (#{aid}, #{uid}, #{category.cid}, #{title}, #{description}, #{picture}, #{content},
                #{commentabled}, #{appreciatabled}, #{created}, #{status})
    </insert>

    <insert id="insertTags">
        insert into bhs_tag_relation(aid, tid)
        values
        <foreach collection="tags" item="tag" separator=",">
            (#{aid}, #{tag.tid})
        </foreach>
    </insert>

    <update id="update">
        update bhs_article
        set cid            = #{category.cid},
            title          = #{title},
            description    = #{description},
            picture        = #{picture},
            content        = #{content},
            commentabled   = #{commentabled},
            appreciatabled = #{appreciatabled},
            status         = #{status}
        where aid = #{aid}
    </update>

    <delete id="delete">
        delete
        from bhs_article
        where aid = #{aid}
    </delete>

    <delete id="deleteList">
        delete
        from bhs_article
        where aid in
        <foreach collection="aids" item="aid" open="(" close=")" separator=",">
            #{aid}
        </foreach>
    </delete>

    <delete id="deleteTagsNotInList">
        delete
        from bhs_tag_relation
        where aid = #{aid}
        and tid in
        (select tid from bhs_tag where name not in
        <foreach collection="tags" item="tag" open="(" close=")" separator=",">
            #{tag}
        </foreach>)
    </delete>

    <select id="selectById" resultMap="articleMap">
        select ba.*, bc.name
        from bhs_article ba
                 left join bhs_category bc on ba.cid = bc.cid
        where aid = #{aid}
    </select>

    <select id="selectAll" resultMap="articleMap">
        select ba.*, bc.name
        from bhs_article ba
                 left join bhs_category bc on ba.cid = bc.cid
    </select>

    <select id="selectAllByStatus" resultMap="articleMap">
        select ba.*, bc.name
        from bhs_article ba
                 left join bhs_category bc on ba.cid = bc.cid
        where status = #{status}
    </select>

    <select id="selectAllByCategoryId" resultMap="articleMap">
        select ba.*, bc.name
        from bhs_article ba
                 left join bhs_category bc on ba.cid = bc.cid
        where ba.cid = #{cid}
    </select>

    <select id="selectAllByUserId" resultMap="articleMap">
        select ba.*, bc.name
        from bhs_article ba
                 left join bhs_category bc on ba.cid = bc.cid
        where uid = #{uid}
    </select>

    <select id="selectAllByTagName" resultMap="articleMap">
        select ba.*, bc.name
        from bhs_article ba
                 left join bhs_category bc on ba.cid = bc.cid
        where aid in
              (select aid
               from bhs_tag_relation
                        inner join bhs_tag bt on bhs_tag_relation.tid = bt.tid
               where name = #{name})
    </select>
</mapper>
